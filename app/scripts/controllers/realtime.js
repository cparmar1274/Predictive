'use strict';

/**
 * @ngdoc function
 * @name predictiveApp.controller:RealtimeCtrl
 * @description
 * # RealtimeCtrl
 * Controller of the predictiveApp
 */
angular.module('predictiveApp')
  .controller('RealtimeCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
