'use strict';

/**
 * @ngdoc function
 * @name predictiveApp.controller:LocalrotueCtrl
 * @description
 * # LocalrotueCtrl
 * Controller of the predictiveApp
 */
angular.module('predictiveApp')
  .controller('LocalrotueCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
