'use strict';

/**
 * @ngdoc function
 * @name predictiveApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the predictiveApp
 */
angular.module('predictiveApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
